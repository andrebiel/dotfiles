Include ~/.ssh/conductor_config

# Machine-local hosts, addresses, users and remote commands live outside the
# repository. Put them in ~/.ssh/config.d/ (created by ./bootstrap link).
Include ~/.ssh/config.d/*
